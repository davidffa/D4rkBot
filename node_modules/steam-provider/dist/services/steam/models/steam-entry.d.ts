/**
 * Class for the info for each search result
 */
export declare class SteamSearchEntry {
    private id;
    private name;
    private url;
    private price;
    private priceWithDiscount;
    private score;
    constructor($id: string, $name: string, $url: string, $price: string, $priceWithDiscount: string, $score: string);
    readonly $id: string;
    readonly $name: string;
    readonly $url: string;
    readonly $price: string;
    readonly $priceWithDiscount: string;
    readonly $score: string;
}
/**
 * Class for the details  of each game
 */
export declare class SteamDetailEntry {
    private id;
    private name;
    private genres;
    private priceData;
    private otherData;
    constructor($id: string, $name: string, $genres: Array<string>, $priceData: PriceInfo, $otherData: OtherInfo);
    readonly $id: string;
    readonly $name: string;
    readonly $genres: Array<string>;
    readonly $priceData: PriceInfo;
    readonly $otherData: OtherInfo;
}
/**
 * Other info inside SteamDetailEntry
 */
export declare class OtherInfo {
    private imageUrl;
    private platforms;
    private metacriticScore;
    private features;
    private developer;
    private publisher;
    constructor($imageUrl: string, $platforms: Array<string>, $metacriticScore: number, $features: Array<string>, $developer: Array<string>, $publisher: Array<string>);
    readonly $imageUrl: string;
    readonly $platforms: Array<string>;
    readonly $metacriticScore: number;
    readonly $features: Array<string>;
    readonly $developer: Array<string>;
    readonly $publisher: Array<string>;
}
/**
 * Pricing info inside SteamDetailEntry
 */
export declare class PriceInfo {
    private initialPrice;
    private finalPrice;
    private discountPercent;
    constructor($initialPrice: string, $finalPrice: string, $discountPercent: number);
    readonly $initialPrice: string;
    readonly $finalPrice: string;
    readonly $discountPercent: number;
}
